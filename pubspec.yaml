name: flutter_scaffold_cli
description: Um CLI para criar módulos Flutter seguindo a estrutura do Flutter Modular com o MobX.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  path: ^1.8.3
  yaml: ^3.1.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

executables:
  flutter_scaffold_cli: fscaffold
